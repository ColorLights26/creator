# Keep this zero-dependency package independent of the host application's lint
# package as well as its runtime dependencies.
linter:
  rules:
    - avoid_relative_lib_imports
    - cancel_subscriptions
    - close_sinks
    - unawaited_futures
