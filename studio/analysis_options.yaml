analyzer:
  language:
    strict-casts: true
    strict-inference: true

linter:
  rules:
    - cancel_subscriptions
    - close_sinks
    - unawaited_futures
    - use_build_context_synchronously
    - avoid_print
